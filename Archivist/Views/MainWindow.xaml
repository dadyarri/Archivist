<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Archivist.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Archivist"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Archivist">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <NavigationView x:Name="MainNavigationView" 
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        PaneDisplayMode="Left"
                        SelectionChanged="MainNavigationView_SelectionChanged">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Add" Content="Создать саммари" Tag="CreateSummary" />
                <NavigationViewItem Icon="List" Content="Список саммари" Tag="ListSummaries" />
                <NavigationViewItem Icon="Setting" Content="Настройки" Tag="Settings" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" 
                   Margin="0,0,0,0"
                   Padding="0,0,0,0" />
        </NavigationView>
    </Grid>
</Window>
