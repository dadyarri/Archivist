<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Archivist.Views.CreateSummaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid HorizontalAlignment="Center" MaxWidth="1200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Main content row with steps side by side -->
            <Grid x:Name="StepsContainer" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Step 1: Folder Selection -->
                <Border Grid.Column="0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8"
                        Padding="20"
                        Height="400">
                    <StackPanel Orientation="Vertical" Spacing="15">
                        <TextBlock Text="1. Выберите папку с аудиофайлами" 
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button x:Name="SelectFolderButton"
                                    Content="Выбрать папку"
                                    Click="SelectFolderButton_Click"
                                    HorizontalAlignment="Left"/>
                            <TextBlock x:Name="SelectedFolderText"
                                       Text="Папка не выбрана"
                                       Style="{StaticResource BodyTextBlockStyle}"
                                       VerticalAlignment="Center"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Step 2: Character Configuration (initially hidden) -->
                <Border x:Name="Step2Border"
                        Grid.Column="2"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8"
                        Padding="20"
                        Height="400"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Vertical" Spacing="15">
                        <TextBlock Text="2. Настройте персонажей для аудиофайлов" 
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        
                        <TextBlock x:Name="NoFilesText"
                                   Text="Сначала выберите папку с аудиофайлами"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Visibility="Visible"/>
                        
                        <ListView x:Name="AudioFilesListView"
                                  Visibility="Collapsed"
                                  MaxHeight="300"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                            CornerRadius="4"
                                            Padding="15"
                                            Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="5">
                                                <TextBlock Text="{Binding FileName}"
                                                           Style="{StaticResource BodyTextBlockStyle}"
                                                           FontWeight="SemiBold"/>
                                                <TextBox x:Name="CharacterNameTextBox"
                                                         Text="{Binding CharacterName, Mode=TwoWay}"
                                                         PlaceholderText="Введите имя персонажа"
                                                         Width="200"
                                                         HorizontalAlignment="Left"/>
                                            </StackPanel>
                                            
                                            <Button Grid.Column="1"
                                                    x:Name="RemoveButton"
                                                    Content="Удалить"
                                                    Click="RemoveButton_Click"
                                                    VerticalAlignment="Center"
                                                    Style="{StaticResource AccentButtonStyle}"
                                                    Padding="10,5"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Progress card (independent, initially hidden) -->
            <Border x:Name="ProgressBorder"
                    Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,20,0,0"
                    Width="800"
                    HorizontalAlignment="Center"
                    Visibility="Collapsed">
                <StackPanel Orientation="Vertical" Spacing="15">
                    <TextBlock Text="Создание саммари..." 
                               x:Name="ProgressStageTitle"
                               Style="{StaticResource SubtitleTextBlockStyle}"/>
                    
                    <ProgressBar x:Name="ProgressBar"
                                 Value="0"
                                 Maximum="100"
                                 Height="8"/>
                    
                    <TextBlock x:Name="ProgressText"
                               Text="Подготовка..."
                               Style="{StaticResource BodyTextBlockStyle}"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"/>
                    
                    <Button x:Name="CancelButton"
                            Content="Отмена"
                            HorizontalAlignment="Center"
                            Click="CancelButton_Click"/>
                </StackPanel>
            </Border>

            <!-- Bottom row with create summary button -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Button x:Name="CreateSummaryButton"
                        Content="Написать саммари"
                        HorizontalAlignment="Center"
                        Click="CreateSummaryButton_Click"
                        IsEnabled="False"
                        Style="{StaticResource AccentButtonStyle}"
                        Padding="20,10"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
